import { ModelFormatPipe } from './model-format.pipe';

describe('ModelFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
